PATCH 88 – fuller left homepage panel, still no scroll at 100% zoom

Changed only the desktop left homepage panel:
- Enlarged the title, poem, dedication, eyebrow and navigation buttons.
- Uses the available vertical space with space-between instead of shrinking everything.
- Keeps the left panel locked to the viewport height and hides internal overflow, so it should not require its own scroll at 100% browser zoom.
- Enlarges the existing corner council logo and moves it inward from the left edge.
- Includes a shorter-laptop fallback so the content still fits on lower-height screens.

Mobile behavior from Patch 87 is unchanged.
