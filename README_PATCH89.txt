PATCH 89 – homepage title/search relocation

Layout-only update. Existing Hebrew content is not rewritten.

- "שער הנגב זוכרת" is moved to the large centered title area.
- The council logo is displayed next to that title.
- "רקמה אנושית אחת" is moved to the left memorial panel immediately before the existing quote.
- The existing search/filter controls are moved into the left panel.
- The existing 2023 / previous-years navigation stays in the left panel.
- No new memorial/footer wording is added.
- Mobile keeps the same content and uses the existing collapsible search behavior.
