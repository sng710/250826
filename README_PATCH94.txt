PATCH 94 – leaf panel + single logo + place dropdown fix

Changes only layout/styling. No memorial wording changed.

- Removed the logo beside “שער הנגב זוכרת”.
- Keeps only the original left-side council logo and enlarges it.
- Removes white/card-like backgrounds so the left controls sit on the leaf background.
- Enlarges the left-side title, quote, and dedication text.
- Opens the settlement combobox upward on desktop with a large internally scrollable list.
- Keeps a mobile-friendly downward dropdown with internal scrolling.
