PATCH 86 – mobile media stacking + collapsible mobile search

Included changes:
1) Person pages on mobile:
   - media items no longer use a horizontal rail.
   - all top media items stack vertically, one under another.
   - this makes it clear when there is more than one video/item.
   - fixes cases like Omer where the first video looked cut and horizontal scrolling was unclear.

2) Homepage on mobile:
   - added a collapsible “חיפוש וסינון” button.
   - search/filter panel opens and closes on tap.
   - desktop behavior stays unchanged.

3) Site version bumped to 284 for cache refresh.
